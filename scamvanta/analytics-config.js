/* Optional GA4 analytics. Disabled until the owner completes README setup.
   Paste your real web-stream Measurement ID in measurementId, then set enabled
   to true. Visitors must also opt in before any Google script is requested. */
window.SCAMVANTA_ANALYTICS_CONFIG = Object.freeze({
  enabled: false,
  measurementId: '' // Paste your G-... Measurement ID between these quotes.
});
